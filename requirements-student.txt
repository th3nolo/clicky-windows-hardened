# Intentionally empty.
#
# This legacy manifest prevents stale instructions from installing an
# unreviewed dependency set. The only supported dependency/build entrypoint is
# `build.bat`, backed by `pyproject.toml` and the hash-bearing `uv.lock`.
